[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "age-of-agents"
version = "0.1.0"
description = "Runnable companion examples for Book 5, The Age of AI Agents."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
